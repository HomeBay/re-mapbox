{
  "name": "re-mapbox",
  "version": "0.1.0",
  "description": "Mapbox GL bindings for ReasonML",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bsb -clean-world -make-world"
  },
  "author": "mlms13",
  "license": "ISC",
  "peerDependencies": {
    "@mapbox/mapbox-gl-draw": "^1.1.1",
    "mapbox-gl": "^0.52.0"
  },
  "devDependencies": {
    "bs-platform": "^5.0.3"
  }
}
